export interface Car {
	id: string,
	modelName: string, //nome do carro 
    bodyType: string, // modelo do carro (suv)
    modelType: string, // vai ao lado do nome do carro 
    imageUrl: string //imagem do carro 
    
    bg1?: string;
    bg2?: string;
    bg3?: string;
  
}
